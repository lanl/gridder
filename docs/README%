The files in this directory are the web pages for the GRIDDER program:

gridder.html        home page
users_manual.html   GRIDDER users manual and tutorial
examples.html       page with examples
example1.html       explains input for example 1 in detail
exmaple2.html       explains input for example 2 in detail

directories: 
gridder.dir:
   gridder.c        source code for GRIDDER program
   gridder          executable GRIDDER program


examples.dir:
  avs.dir:
     example1.input  input file for generating example 1 grid in avs format
     example1.inp    output file for example 1 in avs format
     example2.input  input file for generating example 2 grid in avs format
     example2.inp    output file for example 2 in avs format

  tracer3d.dir 
     example1.input  input file for generating example 1 grid in tracer3d format
     example1.inp    output file for example 1 in avs format
     example2.input  input file for generating example 2 grid in tracer3d format
     example2.inp    output file for example 2 in avs format

